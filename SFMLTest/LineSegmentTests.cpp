#include "LineSegmentTests.h"
#include "LineSegment.h"

using namespace SFMLTest;

void LineSegmentTests::RunTests()
{
	//Intersection test
	/*LineSegment segment1 = LineSegment(sf::Vector2f(0, 0), sf::Vector2f(5, 5));
	LineSegment segment2 = LineSegment(sf::Vector2f(5, 0), sf::Vector2f(0, 5));

	sf::Vector2f intersectionPoint;

	LineSegment::IntersectionType intersection = segment1.IntersectsWith(segment2, intersectionPoint);

	//Overlap test
	LineSegment segment3 = LineSegment(sf::Vector2f(2,2), sf::Vector2f(7,7));

	intersection = segment1.IntersectsWith(segment3, intersectionPoint);

	//No intersection test
	LineSegment segment4 = LineSegment(sf::Vector2f(7, 8), sf::Vector2f(2, 10));

	intersection = segment1.IntersectsWith(segment4, intersectionPoint);

	//Vertical and horizontal line.
	LineSegment segment5 = LineSegment(sf::Vector2f(3.5, 3.5), sf::Vector2f(3.5, 8.5));
	LineSegment segment6 = LineSegment(sf::Vector2f(0.5, 5.5), sf::Vector2f(8.5, 5.5));

	intersection = segment5.IntersectsWith(segment6, intersectionPoint);

	int debug = 0;*/
}